<div class="container-fluid">
    <div class="card card-body blur shadow-blur overflow-hidden">
      <div class="row gx-4">
        <div class="col-auto my-auto">
          <div class="h-100">
            <h5 class="mb-1 icon-move-right">
              Gestionnaires <i class="fas fa-arrow-right text-sm ms-1" aria-hidden="true"></i> <b class="text-primary">Consultation & Ajout</b>
            </h5>
            <p class="mb-0 font-weight-bold text-sm">
              Consultation, Ajout et Modification
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="container-fluid py-4">
    <div class="row">
      <div class="col-12 mt-4">
        <div class="card mb-4">
          <div class="card-header pb-0 p-3">
            <p class="text-sm">Liste des Gestionnaires</p>
            {% for message in app.flashes('admin-error') %}
              <div class="alert alert-sm alert-danger" role="alert">
                <strong class="text-white">{{message}}</strong>
              </div>
            {% endfor %}
  
            {% for message in app.flashes('admin-success') %}
            <div class="alert alert-sm alert-success" role="alert">
              <strong class="text-dark">{{message}}</strong>
            </div>
          {% endfor %}
  
          </div>
          <div class="card-body p-3">
            <div class="row">
              {% include './_partials/managers/list/modal.html.twig' %}
              {% include './_partials/managers/list/map-admins.html.twig' %}
  
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  