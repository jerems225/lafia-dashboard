<div class="container-fluid">
    <div class="card card-body blur shadow-blur overflow-hidden">
      <div class="row gx-4">
        <div class="col-auto my-auto">
          <div class="h-100">
            <h5 class="mb-1 icon-move-right">
              Livreurs <i class="fas fa-arrow-right text-sm ms-1" aria-hidden="true"></i> <b class="text-primary"> Demandes d'adhesion</b>
            </h5>
            <p class="mb-0 font-weight-bold text-sm">
              Consultation et Changement du statut
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="container-fluid py-4">
    <div class="row">
      <div class="col-12 mt-4">
        <div class="card mb-4">
          <div class="card-header pb-0 p-3">
            <p class="text-sm">Liste des demandes</p>
            {% for message in app.flashes('riders-error') %}
              <div class="alert alert-sm alert-danger" role="alert">
                <strong class="text-white">{{message}}</strong>
              </div>
            {% endfor %}
  
            {% for message in app.flashes('edit-rider') %}
            <div class="alert alert-sm alert-success" role="alert">
              <strong class="text-dark">{{message}}</strong>
            </div>
          {% endfor %}
  
          </div>
          <div class="card-body p-3">
            <div class="row">
  
              {% include './_partials/riders/list/map-riders-request.html.twig' %}
  
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  